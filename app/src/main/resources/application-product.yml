spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://<host>:<port>/<db>
    username: <name>
    password: <pass>
  mvc:
    hiddenmethod:
      filter:
        enabled: true
